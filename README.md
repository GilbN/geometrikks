# GeoMetrikks

GeoMetrikks ingests nginx logs, extracts geolocation metrics, and stores them in a PostGIS database. The backend is built with Litestar, and the frontend uses React, Vite, and TanStack Query etc. Data is visualized in a web dashboard.

**Status:** ⚠️ This project is in development. Features and APIs will change.
